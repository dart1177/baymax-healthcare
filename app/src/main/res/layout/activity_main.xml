<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/baymax_background">

    <!-- Baymax Face Container -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <!-- Baymax Head Circle -->
        <View
            android:id="@+id/baymaxHead"
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_centerInParent="true"
            android:background="@drawable/baymax_head_shape"
            android:elevation="8dp" />

        <!-- Baymax Eyes Container -->
        <LinearLayout
            android:id="@+id/eyesContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginTop="-20dp"
            android:orientation="horizontal"
            android:gravity="center">

            <!-- Left Eye -->
            <com.baymax.healthcare.BaymaxEye
                android:id="@+id/leftEye"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginEnd="60dp" />

            <!-- Right Eye -->
            <com.baymax.healthcare.BaymaxEye
                android:id="@+id/rightEye"
                android:layout_width="40dp"
                android:layout_height="40dp" />

        </LinearLayout>

        <!-- Baymax Mouth -->
        <View
            android:id="@+id/baymaxMouth"
            android:layout_width="60dp"
            android:layout_height="15dp"
            android:layout_below="@id/eyesContainer"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="40dp"
            android:background="@drawable/baymax_mouth_shape"
            android:elevation="6dp" />

        <!-- Subtle glow effect when listening -->
        <View
            android:id="@+id/glowEffect"
            android:layout_width="320dp"
            android:layout_height="320dp"
            android:layout_centerInParent="true"
            android:background="@drawable/baymax_glow_effect"
            android:alpha="0.0"
            android:elevation="4dp" />

    </RelativeLayout>

</FrameLayout>
